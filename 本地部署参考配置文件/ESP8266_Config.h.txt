#ifndef __ESP8266_Config_H
#define __ESP8266_Config_H

#include "stm32f10x.h"

extern char WIFI_SSID[32];       // WiFi-SSID
extern char WIFI_PWD[64];        // WiFi-Password
extern char SERVER_IP[32];       // 服务器地址
extern uint16_t SERVER_PORT;     // 端口号 (uint16_t 足够存储 0-65535)
extern uint16_t UPLOAD_INTERVAL; // 间隔时间 (单位ms)

#endif
